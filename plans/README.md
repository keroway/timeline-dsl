<!-- markdownlint-disable MD013 MD060 -->

# Advisor Plans

Generated by the improve skill. Execute TODO plans in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, run every verification gate, and update the status row when done.

## Execution order & status

| ID | Plan | Priority | Effort | Status | Dependencies |
|---|---|---|---|---|---|
| 001 | [Fix minute/hour precision validation](001-fix-minute-hour-precision-validation.md) | P1 | S | DONE | None |
| 002 | [Synchronize stale documentation with implemented DSL/LSP/formatter behavior](002-sync-stale-docs.md) | P1 | M | DONE | None |
| 003 | [Add CI coverage for VS Code extension tests](003-add-vscode-extension-ci.md) | P1 | S | DONE | None |
| 004 | [Harden VS Code publish dependency install](004-harden-vscode-publish-dependency-install.md) | P1 | S | DONE | None |
| 005 | [Propagate DSL color_map through WASM and WebUI rendering](005-propagate-color-map-through-wasm-webui.md) | P1 | S | DONE | None |
| 006 | [Validate color_map CSS values before emitting SVG style attributes](006-validate-color-map-css-values.md) | P1 | M | DONE | None; pairs well with 005 |
| 007 | [Preserve WebUI preview render options in HTML and PDF export](007-preserve-webui-render-options-in-export.md) | P1 | S | DONE | None; benefits from 005 |
| 008 | [Improve WebUI keyboard accessibility for preview and split pane controls](008-improve-webui-keyboard-accessibility.md) | P2 | M | DONE | None |
| 009 | [Add a typed i18n foundation for WebUI strings](009-add-webui-i18n-foundation.md) | P2 | M | DONE | None |
| 010 | [Harden GitHub Pages deploy against transient failures](010-harden-github-pages-deploy.md) | P2 | S | TODO | None |
| 011 | [Complete the WebUI i18n migration (hardcoded JA strings remain)](011-complete-webui-i18n-migration.md) | P1 | M | TODO | Builds on 009 (merged) |
| 012 | [Replace native window.confirm with in-app accessible modal](012-replace-native-confirm-with-in-app-modal.md) | P2 | S | TODO | Pairs with 011 |
| 013 | [Move WebUI WASM compile/render into a Web Worker](013-webui-wasm-web-worker.md) | P2 | M | TODO | Sequence after 011/012 |
| 014 | [Investigate/resolve memmap2 unsound advisory in PDF chain](014-investigate-memmap2-unsound-advisory.md) | P3 | S | TODO | None |
| 015 | [Rust code-quality polish batch (decompile/LSP/CLI tests)](015-rust-code-quality-polish.md) | P3 | S | DONE | None; Items A/B were already resolved pre-plan (see PR) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Recommended order

1. **005** first: fixes CLI/WebUI render parity for a documented DSL feature.
2. **006** next or in parallel: hardens the same `color_map` surface. It can land before 005, but reviewing both together is efficient.
3. **007** after or alongside 005: makes WebUI exports match preview options.
4. **008** and **009** can proceed independently once the render-parity fixes are not occupying the same reviewers.

### Batch 2 (plans 010–015, added 2026-07-05 at commit `1c22dac`; issues #588–#593)

1. **011** first among the WebUI set: highest value (finishes the half-done i18n so the `en` locale is real). Land in reviewable slices (hooks → components → editor).
2. **012** right after 011: reuses 011's new i18n keys; removes the last `window.confirm` calls.
3. **013** after 011/012: touches the same WASM/compiler files, so sequencing avoids churn.
4. **010** anytime (isolated CI change; unblocks the red Pages badge).
5. **014** and **015** are independent low-priority hygiene; pick up when convenient.

## Dependency notes

- 005, 006, and 007 all touch render/WebUI behavior but do not strictly depend on each other. If multiple executors work in parallel, coordinate to avoid conflicts in `crates/tdsl-wasm/src/lib.rs`, `apps/webui/src/App.tsx`, and `apps/webui/src/hooks/useExport.ts`.
- 008 and 009 both touch WebUI components. Prefer landing 008 before 009 if both are implemented by different agents, because 009 may change many labels/props and increase merge conflicts.

## Verification baseline

Use these gates unless an individual plan narrows them further:

- Rust: `cargo fmt --all -- --check`, `cargo test --workspace --all-targets`
- WebUI: `cd apps/webui && npm run lint && npm test && npm run build`
- VS Code extension: `cd editors/vscode && npm test && npm run compile` when editor files are touched
- WASM: `wasm-pack build crates/tdsl-wasm --target web` when `tdsl-wasm` exports or bindings are touched

## Findings considered and rejected

- CSV `source`/`origin` full round-trip remains intentionally unimplemented and is already documented as a future extension.
- ~~`cargo audit` reported `memmap2 0.9.10` as an unsound transitive dependency~~ — now planned as **014**.
- ~~Full Web Worker migration for WebUI WASM rendering~~ — now planned as **013**.
- The `gen-grammar-keywords.mjs` brittleness note from the 2026-06-10 review is **resolved**: the generator reads `apps/webui/src/lang-tdsl/keywords.json` as the single source (verified 2026-07-05). Do not re-open.
- GitHub Pages deploy failure on run #586 was investigated (2026-07-05): a **transient** `Deployment failed, try again later` backend error, not a build regression (artifact uploaded, site live). Hardening tracked as **010**.
